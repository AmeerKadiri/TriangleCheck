#include <iostream>
#include <string>
#include <iomanip>
#include <fstream>
#include <cmath>
using namespace std;

int main(){

double isoscelese, equilateral, right, scalene, nt, both;
double a, b, c;

cout<<"Enter the Length, Width and Height of a triangle"<<endl;
cin >> a >> b >> c;

if ((a) )pow(2) * (b) pow(2) = (c) pow(2)){
  //right
  if (a = b) {
    cout<<"Your triangle is a right triangle";
  else {
    cout<<"Your triangle is not a right triangle"
  }
  }

}




}
return0;
